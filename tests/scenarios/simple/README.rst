Simple setup scenario
=====================

This is your typical small project, with just:
- one ``simple.py`` module (no module folder)
- a ``README.rst`` file
- everything (except classifiers) comes from ``simple.py``
- note that 'download_url' in simple.py mentions "{version}"
