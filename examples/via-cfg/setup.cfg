[metadata]
name = via-cfg
version = file: VERSION
author = Someone someone@example.org

[options]
zip_safe = no
python_requires = >=3.7
setup_requires =
    setupmeta

[options.entry_points]
console_scripts =
	pytest=pytest:main
